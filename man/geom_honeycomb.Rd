% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_honeycomb.R
\docType{data}
\name{geom_honeycomb}
\alias{geom_honeycomb}
\alias{GeomHoneycomb}
\title{Honeycomb waffle chart}
\usage{
geom_honeycomb(
  mapping = NULL,
  data = NULL,
  stat = "honeycomb",
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  values_are = c("auto", "counts", "proportions"),
  n_cells = NULL,
  silhouette = c("rect", "rounded", "organic"),
  layout = c("compact", "free"),
  compact_style = c("perimeter", "blocky"),
  temperature = NULL,
  min_width = NULL,
  max_width = NULL,
  min_height = NULL,
  max_height = NULL,
  seed = NULL,
  rotation = 0,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
\strong{Required}: \code{fill} (category) and \code{weight} (numeric values).}

\item{data}{The data to be displayed in this layer.}

\item{stat}{The statistical transformation to use on the data.
Default is \code{"honeycomb"}.}

\item{position}{Position adjustment, either as a string or the result of
a position adjustment function. Not typically used for honeycomb plots.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}

\item{values_are}{How to interpret the \code{weight} aesthetic values:
\describe{
\item{\code{"auto"}}{(Default) If weights sum to 1 (within tolerance 1e-6),
treat as proportions; otherwise treat as counts.
}
\item{\code{"counts"}}{Treat weights as raw counts to be normalized.}
\item{\code{"proportions"}}{Treat weights as proportions (must sum to 1).}
}}

\item{n_cells}{Number of hexagonal cells to use. If \code{NULL} (default),
automatically chosen based on the number of categories and minimum
proportion. Maximum allowed is 2500.}

\item{silhouette}{Shape of the overall honeycomb boundary.
Accepted values: \code{"rect"} (default), \code{"rounded"}, \code{"organic"}.
All three silhouettes produce a connected mask of the requested size.
\code{"rounded"} softens the corners of a rectangular grid; \code{"organic"}
grows an irregular boundary from a central seed.}

\item{layout}{Region allocation strategy.
Accepted values: \code{"compact"} (default), \code{"free"}.
\code{"compact"} assigns cells deterministically (\code{temperature} must be 0);
\code{"free"} uses stochastic top-K sampling controlled by \code{temperature}.}

\item{compact_style}{Style of region packing when \code{layout = "compact"}.
Accepted values: \code{"perimeter"} (default), \code{"blocky"}.
\code{"perimeter"} minimises region perimeter via greedy growth with a
carve fallback; \code{"blocky"} uses directional face-carving.
\code{"blocky"} requires \code{silhouette = "rect"}.}

\item{temperature}{Controls randomness in region allocation. A single
non-negative number:
\describe{
\item{\code{NULL}}{(Default) Automatically set: 0 for \code{layout = "compact"},
0.35 for \code{layout = "free"}.}
\item{\code{0}}{Deterministic allocation. Required when \code{layout = "compact"}.}
\item{\verb{> 0}}{Increasing randomness. Only valid with \code{layout = "free"}.}
}}

\item{min_width, max_width}{Constraints on grid width (number of columns).
Default \code{NULL} means no constraint.}

\item{min_height, max_height}{Constraints on grid height (number of rows).
Default \code{NULL} means no constraint.}

\item{seed}{Random seed for reproducible layouts. If \code{NULL}, uses a
random seed.}

\item{rotation}{Rotation of the rendered honeycomb in degrees.
Must be one of \code{0}, \code{90}, \code{180}, \code{270}. Rotation is applied to output
coordinates only (grid allocation/contiguity are unchanged).}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}. Passing the
removed argument will raise an error with migration guidance.}
}
\value{
A ggplot2 layer that can be added to a plot.
}
\description{
\code{geom_honeycomb()} creates a honeycomb-style waffle chart where hexagonal
cells represent proportional data. Each category forms a contiguous region
of connected hexagons, making it easy to compare relative sizes.
}
\section{Aesthetics}{

\code{geom_honeycomb()} understands the following aesthetics:

\strong{Required aesthetics:}
\describe{
\item{\code{fill}}{The categorical variable defining groups. Each unique value
gets its own contiguous region of hexagonal cells.}

\item{\code{weight}}{Numeric values representing counts or proportions for each
category. These determine how many cells each category receives.
\strong{This aesthetic is required} and the geom will error if not provided.}
}

\strong{Optional aesthetics:}
\describe{
\item{\code{colour}}{Border color of hexagonal cells. Default is \code{NA} (no border).}
\item{\code{alpha}}{Transparency of fill color. Default is 1 (opaque).}
\item{\code{linewidth}}{Width of cell borders. Default is 0.5.
(Note: only visible if \code{colour} is set.)}
\item{\code{linetype}}{Type of cell borders. Default is 1 (solid).}
}

\strong{Ignored aesthetics:}
\describe{
\item{\code{x}, \code{y}}{These are \strong{ignored} because the honeycomb layout is
computed internally based on the weights. You do not need to (and should
not) map variables to x or y.}
}
}

\section{Value Interpretation}{

The \code{values_are} parameter controls how \code{weight} values are interpreted:

\describe{
\item{\code{"auto"} (default)}{If weights sum to 1 (within tolerance 1e-6),
they are treated as proportions. Otherwise, they are treated as counts
and normalized internally.}
\item{\code{"counts"}}{Weights are raw counts (e.g., 40, 30, 20, 10).
They will be normalized to proportions internally.
}
\item{\code{"proportions"}}{Weights are proportions that must sum to 1
(e.g., 0.4, 0.3, 0.2, 0.1). An error is raised if they don't.}
}
}

\section{Layout Algorithm}{

The honeycomb layout ensures:
\itemize{
\item Each category forms exactly one contiguous region (connected hexagons)
\item Cell counts are proportional to weights (using largest-remainder rounding)
\item Every non-zero category gets at least one cell
\item Layouts are reproducible when \code{seed} is set
}
}

\section{Interactive Use}{

The stat computes \code{tooltip_label} and \code{pct}, which can be used with optional
interactive backends such as \code{ggiraph}. Keep core plots static; when you need
tooltips, you can layer \code{ggiraph::geom_polygon_interactive()} and map
\code{ggplot2::after_stat(tooltip_label)}.
}

\examples{
\dontrun{
library(ggplot2)

# Basic honeycomb with counts
pollen_data <- data.frame(
  species = c("Clover", "Wildflower", "Lavender", "Sunflower", "Other"),
  count = c(45, 25, 15, 10, 5)
)

ggplot(pollen_data, aes(fill = species, weight = count)) +
  geom_honeycomb() +
  coord_equal()

# With proportions (explicit)
pollen_prop <- data.frame(
  species = c("Clover", "Wildflower", "Lavender", "Sunflower", "Other"),
  prop = c(0.45, 0.25, 0.15, 0.10, 0.05)
)

ggplot(pollen_prop, aes(fill = species, weight = prop)) +
  geom_honeycomb(values_are = "proportions") +
  coord_equal()

# Control cell count and silhouette
ggplot(pollen_data, aes(fill = species, weight = count)) +
  geom_honeycomb(n_cells = 50, silhouette = "rounded", layout = "free", seed = 42) +
  coord_equal()

# Optional tooltip usage with ggiraph
if (requireNamespace("ggiraph", quietly = TRUE)) {
  ggplot(pollen_data, aes(fill = species, weight = count)) +
    ggiraph::geom_polygon_interactive(
      stat = "honeycomb",
      aes(tooltip = ggplot2::after_stat(tooltip_label))
    ) +
    coord_equal()
}

# With grid constraints
ggplot(pollen_data, aes(fill = species, weight = count)) +
  geom_honeycomb(min_width = 8, max_width = 12) +
  coord_equal()
}

}
\seealso{
\itemize{
\item \code{\link[=stat_honeycomb]{stat_honeycomb()}} for the underlying stat
\item \code{\link[=scale_honey]{scale_honey()}} for honey-inspired discrete scales
\item \code{\link[=theme_honeycomb]{theme_honeycomb()}} for a minimal theme suited to honeycomb plots
}
}
\keyword{datasets}
