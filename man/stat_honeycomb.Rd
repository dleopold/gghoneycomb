% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_honeycomb.R
\docType{data}
\name{stat_honeycomb}
\alias{stat_honeycomb}
\alias{StatHoneycomb}
\title{Compute honeycomb layout statistics}
\usage{
stat_honeycomb(
  mapping = NULL,
  data = NULL,
  geom = "honeycomb",
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  values_are = c("auto", "counts", "proportions"),
  n_cells = NULL,
  compaction = 1,
  min_width = NULL,
  max_width = NULL,
  min_height = NULL,
  max_height = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
\strong{Required}: \code{fill} (category) and \code{weight} (numeric values).}

\item{data}{The data to be displayed in this layer.}

\item{geom}{The geometric object to use to display the data.}

\item{position}{Position adjustment, either as a string or the result of
a position adjustment function. Not typically used for honeycomb plots.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}

\item{values_are}{How to interpret the \code{weight} aesthetic values:
\describe{
\item{\code{"auto"}}{(Default) If weights sum to 1 (within tolerance 1e-6),
treat as proportions; otherwise treat as counts.
}
\item{\code{"counts"}}{Treat weights as raw counts to be normalized.}
\item{\code{"proportions"}}{Treat weights as proportions (must sum to 1).}
}}

\item{n_cells}{Number of hexagonal cells to use. If \code{NULL} (default),
automatically chosen based on the number of categories and minimum
proportion. Maximum allowed is 2500.}

\item{compaction}{Controls the regularity of the honeycomb boundary:
\describe{
\item{\code{1}}{(Default) Rectangular bounding grid.}
\item{\verb{< 1}}{Increasingly irregular silhouette via boundary erosion.}
}
Must be in the range (0, 1].}

\item{min_width, max_width}{Constraints on grid width (number of columns).
Default \code{NULL} means no constraint.}

\item{min_height, max_height}{Constraints on grid height (number of rows).
Default \code{NULL} means no constraint.}

\item{seed}{Random seed for reproducible layouts when \code{compaction < 1}.
If \code{NULL}, uses a random seed.}

\item{...}{Other arguments passed to the stat.}
}
\value{
A ggplot2 layer that can be added to a plot.
}
\description{
\code{stat_honeycomb()} computes the layout for a honeycomb waffle chart,
transforming categorical data with associated weights into hexagonal cell
positions. This stat is typically used with \code{\link[=geom_honeycomb]{geom_honeycomb()}}.
}
\section{Data Contract}{

The stat expects data with:
\itemize{
\item A categorical variable mapped to \code{fill} (the category for each cell)
\item A numeric variable mapped to \code{weight} (counts or proportions)
}

The stat \strong{ignores} any \code{x} and \code{y} mappings in the input data because
the honeycomb layout is computed internally based on the weights.
}

\section{Computed Variables}{

The stat computes the following variables for use by the geom:
\describe{
\item{x, y}{Vertex coordinates for each hexagon}
\item{group}{Unique cell identifier (one per hexagon)}
\item{cell_id}{Same as group, explicit cell identifier}
\item{grid_col, grid_row}{Integer grid coordinates (odd-q offset indices)}
\item{pct}{Normalized proportion for the category}
\item{tooltip_label}{Preformatted string "\if{html}{\out{<category>}}: \if{html}{\out{<pct>}}\%"}
}
}

\examples{
\dontrun{
library(ggplot2)

# Example with counts
df <- data.frame(
  category = c("A", "B", "C", "D"),
  count = c(40, 30, 20, 10)
)

ggplot(df, aes(fill = category, weight = count)) +
  stat_honeycomb()

# Example with proportions
df_prop <- data.frame(
  category = c("A", "B", "C", "D"),
  prop = c(0.4, 0.3, 0.2, 0.1)
)

ggplot(df_prop, aes(fill = category, weight = prop)) +
  stat_honeycomb(values_are = "proportions")
}

}
\seealso{
\code{\link[=geom_honeycomb]{geom_honeycomb()}} for the geom that draws the hexagons.
}
\keyword{datasets}
